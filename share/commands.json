{
  "_schema": {
    "mutates": "true when running the command as shown, with no further interactive confirmation, changes persistent state: the deck's zellij session, a systemd unit, the profile, or any file phosphor generates -- or otherwise leaves this machine different than before. false is the safe-by-default reading: some entries still name a flag or an interactive key that escalates past it, in \"note\".",
    "needs_deck": "true when the command needs the phosphor session already running, or to be run from inside it, to do anything meaningful.",
    "note": "one line of nuance: which flag or interactive key changes the plain answer above, and how."
  },
  "adjutant": {"mutates": false, "needs_deck": false,
    "note": "SYS display of local fleet/notes state; its zellij calls (jump to a tab, hide a toast) only fire on a keypress and are no-ops without a live session."},
  "ask": {"mutates": false, "needs_deck": false,
    "note": "shells out to an installed assistant CLI for one answer and prints it; nothing is written locally."},
  "attach": {"mutates": false, "needs_deck": true,
    "note": "waits (up to ~90s) for the session to come up if it's starting, then attaches; a first-ever run also asks one Enter before writing a small 'welcomed' marker, skipped and unwritten without a tty."},
  "ci": {"mutates": false, "needs_deck": false,
    "note": "read-only GitHub/GitLab pipeline status."},
  "clip": {"mutates": false, "needs_deck": false,
    "note": "sends to every attached screen's OSC 52 clipboard; `--save FILE` writes that file locally instead."},
  "commands": {"mutates": false, "needs_deck": false,
    "note": "a category/command picker; a read-only leaf runs on Enter (this same file decides which), anything else just copies its usage line -- never guesses at missing arguments."},
  "completion": {"mutates": false, "needs_deck": false,
    "note": "prints a shell completion snippet."},
  "demo": {"mutates": true, "needs_deck": false,
    "note": "starts (or, with --stop, tears down) a separate throwaway zellij session and its own cache state -- isolated from your real profile/session, but a real one while it runs."},
  "doctor": {"mutates": false, "needs_deck": false,
    "note": "read-only preflight checks, including ssh probes to fleet hosts."},
  "down": {"mutates": true, "needs_deck": false,
    "note": "stops the session and its timer/service units and reaps leftover processes, unconditionally."},
  "edit": {"mutates": true, "needs_deck": true,
    "note": "needs to run inside the deck ($ZELLIJ); the first step already unlocks and renames the tab, and every action after (save, put back, swap, split, close) mutates the tab or profile on one keypress, no further confirmation."},
  "face": {"mutates": true, "needs_deck": false,
    "note": "writes ~/.local/share/phosphor/faces/NAME.json unconditionally."},
  "fleet": {"mutates": true, "needs_deck": false,
    "note": "the SYS fleet panel: reads hosts over ssh, but its background watchdog also self-heals a dead ~/fleet mount on its own (fusermount, then a systemctl restart) and calls phosphor notify on a host's up/down flip."},
  "gen": {"mutates": true, "needs_deck": false,
    "note": "writes zellij/yazi/btop/gping/ctop config, layouts, systemd units and mounts, and restarts fleet mounts if rclone.conf changed; --dry-run writes nothing."},
  "glance": {"mutates": false, "needs_deck": false,
    "note": "read-only summary from local cache files (fleet, mentions, notes, workspace status)."},
  "init": {"mutates": false, "needs_deck": false,
    "note": "the setup wizard: every write happens after an explicit 'write the profile?' Enter, so a non-interactive run aborts at the first prompt instead of writing anything."},
  "keep": {"mutates": false, "needs_deck": true,
    "note": "reading the tab needs the deck (zellij dump-layout); writing it into the profile needs its own explicit 'keep it?' Enter, aborting instead of writing without one."},
  "keys": {"mutates": false, "needs_deck": false,
    "note": "read-only key guide (its interactive 's' just launches phosphor setup)."},
  "logs": {"mutates": false, "needs_deck": false,
    "note": "read-only tail of the deck's own log."},
  "mentions": {"mutates": false, "needs_deck": false,
    "note": "the plain feed is read-only; `--setup` unconditionally writes a hook script and rewrites Mattermost's config.ini; the interactive viewer mutates (marks seen, runs `prepare`) on a keypress."},
  "new": {"mutates": false, "needs_deck": true,
    "note": "with no `--here`: lists (non-tty) or opens a picker that adds a tab on selection; `--here` (what Alt-n and the + menu actually run) refuses instead of adding a tab if nobody's attached to the session (zellij action list-clients names none)."},
  "note": {"mutates": true, "needs_deck": false,
    "note": "appends to the shared notebook on every invocation, no confirmation."},
  "notes": {"mutates": false, "needs_deck": false,
    "note": "plain/non-interactive is read-only; the interactive viewer's write/edit/archive/done/restore/chat/workspace keys each mutate on one keypress, mostly with no secondary confirm ('D', delete for good, does ask first)."},
  "notify": {"mutates": false, "needs_deck": false,
    "note": "writes one ephemeral events-file line; pushes or speaks only if [push]/[tts] are already on -- no session, systemd or profile change."},
  "panel": {"mutates": false, "needs_deck": false,
    "note": "the DECK tab: needs a tty, and every action needs its own keypress; the riskiest ones (restart, update, web on) ask to confirm first."},
  "path": {"mutates": false, "needs_deck": false,
    "note": "pure string translation, no I/O."},
  "phone": {"mutates": false, "needs_deck": false,
    "note": "prints instructions or a kit script for a new screen; the real changes (keys, ~/.local/bin/deck) happen on that remote device when the printed script runs there, not here."},
  "privacy": {"mutates": false, "needs_deck": false,
    "note": "scanning is read-only; `--install` writes .git/hooks/pre-commit unconditionally."},
  "prom": {"mutates": false, "needs_deck": false,
    "note": "read-only Prometheus queries."},
  "pulse": {"mutates": false, "needs_deck": false,
    "note": "read-only display of the local fleet cache."},
  "push": {"mutates": false, "needs_deck": false,
    "note": "status/--qr only read the profile and print/clip a URL; the actual send only happens from `phosphor notify`."},
  "receive": {"mutates": true, "needs_deck": false,
    "note": "opens a one-time HTTP upload link/QR (LAN or tailnet, never the internet); the uploaded file lands in ~/received (or --dir) the moment someone sends one, and it stops the instant that happens or after --timeout."},
  "recipe": {"mutates": false, "needs_deck": false,
    "note": "with no NAME: lists what's already added (read-only, though the interactive picker adds on selection with no confirm); `phosphor recipe NAME` drops the bundle into tabs.d and regenerates unconditionally."},
  "restart": {"mutates": true, "needs_deck": false,
    "note": "down then up, unconditionally, no confirmation; run from inside the live session it detaches and finishes in the background."},
  "review": {"mutates": false, "needs_deck": false,
    "note": "listing/diffing is read-only; the interactive 't' (checkout into its own worktree) and 'x' (drop it) mutate on a keypress -- your own working copy is never touched."},
  "screen": {"mutates": false, "needs_deck": false,
    "note": "same as phone: prints a kit script; the real changes happen on the remote device that runs it."},
  "screens": {"mutates": false, "needs_deck": false,
    "note": "listing is read-only; kicking a screen loose needs 'x' pressed twice, on purpose."},
  "send": {"mutates": true, "needs_deck": false,
    "note": "opens a real (if temporary) HTTP listener serving the file until it's downloaded or the timeout passes."},
  "services": {"mutates": false, "needs_deck": false,
    "note": "read-only systemd unit status."},
  "setup": {"mutates": false, "needs_deck": false,
    "note": "same wizard pattern as init: every change needs its own Enter, aborting on EOF before anything is written."},
  "shortcuts": {"mutates": false, "needs_deck": false,
    "note": "--kdl and non-tty are read-only; the interactive picker writes [keys] and regenerates once a new key is captured."},
  "store": {"mutates": false, "needs_deck": false,
    "note": "listing is read-only; picking an uninstalled app in the interactive picker downloads and installs it immediately, no confirm ('d', remove, does ask first)."},
  "tabs": {"mutates": false, "needs_deck": false,
    "note": "listing is read-only; `--forget NAME` mutates unconditionally, and the interactive view's forget/reorder/reopen keys mutate on one keypress."},
  "trace": {"mutates": false, "needs_deck": false,
    "note": "with no TOOL: lists what's tracing (read-only); `phosphor trace TOOL` writes a trace-control file that expires itself in about 30 minutes."},
  "tts": {"mutates": false, "needs_deck": false,
    "note": "speaking a message or checking status changes nothing; `install` downloads and installs a TTS engine, and `on`/`off` rewrite [tts] in the profile."},
  "tunnel": {"mutates": false, "needs_deck": false,
    "note": "listing is read-only; `on HOST`/`off HOST` write the profile and start/stop a systemd unit that keeps `ssh -N HOST` running, unconditionally."},
  "up": {"mutates": true, "needs_deck": false,
    "note": "enables and starts the session, timer and mount units, unconditionally."},
  "update": {"mutates": true, "needs_deck": false,
    "note": "pulls or copies the new code, runs the installer, and by default restarts the live session (or hotswaps it) with no confirmation; --no-restart is the only opt-out."},
  "version": {"mutates": false, "needs_deck": false,
    "note": "reads a small local cache; --check also does a `git fetch` (network only, no local mutation)."},
  "web": {"mutates": false, "needs_deck": false,
    "note": "status (the default) is read-only; `on` writes [deck].web, regenerates, and publishes a tailnet HTTPS endpoint, then pauses for Enter and restarts the live session -- without a tty it publishes but does NOT restart unless --yes is also given; `off`/`token` mutate unconditionally."},
  "workspace": {"mutates": false, "needs_deck": false,
    "note": "`list` (the default) is read-only; `open` needs the deck ($ZELLIJ) to actually switch tabs, otherwise it just prints; `new` creates the folder, commits it, adds the tab to the profile and regenerates -- non-interactively it also skips the 'folder already has stuff in it' check."}
}
